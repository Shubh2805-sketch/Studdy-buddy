name: study_buddy_app
description: StudyBuddy - Class 12 Science & Maths AI Tutor
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_generative_ai: ^0.4.0
  flutter_tts: ^4.0.2
  speech_to_text: ^6.6.0
  camera: ^0.10.5+9
  shared_preferences: ^2.2.2
  flutter_math_fork: ^0.7.2
  cupertino_icons: ^1.0.6
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
